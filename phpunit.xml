<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>

    <php>
        <!-- Environnement de test -->
        <env name="APP_ENV" value="testing"/>
        <env name="APP_DEBUG" value="true"/>
        <env name="BCRYPT_ROUNDS" value="4"/>

        <!-- Base de données : PostgreSQL (même moteur que production)
             Les valeurs réelles sont injectées par :
             - le CI (variables d'environnement GitHub Actions)
             - ou un fichier .env.testing en local
             Ces valeurs servent de fallback si aucune variable n'est définie. -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="5432"/>
        <env name="DB_DATABASE" value="docsecur_test"/>
        <env name="DB_USERNAME" value="docsecur_ci"/>
        <env name="DB_PASSWORD" value="ci_secret_password"/>
        <env name="DB_SSLMODE" value="disable"/>

        <!-- Drivers rapides pour les tests -->
        <env name="CACHE_DRIVER" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>

        <!-- Swagger -->
        <env name="L5_SWAGGER_CONST_HOST" value="http://localhost"/>

        <!-- Désactiver les fonctionnalités non testées -->
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
